Provide an independent deep engineering audit of common methods for a Linux desktop streaming setup assistant using the PUBLIC open-source Sunshine and Moonlight iOS projects. No private code or project specification is included in this question. Work from general expertise and the public project facts below. Do not use tools or read local files. Do not claim to have tested or browsed. Mark uncertain external facts as needing verification. Return 2500-4000 words of useful ranked advice, concrete failure examples, mitigations, and a regression/manual acceptance matrix. Do not use em-dashes.

Discuss Sunshine installation orchestration and failure reporting, current-process versus stale-log detection, session-count errors with multiple clients, credential existence checks that do not access passwords/hashes, client count errors, service/autostart duplication, ownership-safe idle inhibitors across crashes/restarts, frontend action queues and asynchronous results, bounded no-scroll error presentation on 1280x800, app-install QR codes versus plain-IP QR camera handling on iPad, possible Moonlight iOS host-add deep links, keyboard/mouse support and command-key limitations, fast returning-user UX versus first-time guided progression, and tests possible without access to a physical iPad. Separate provable implementation principles from version-dependent external facts. Explain where an assistant should stop rather than attempt arbitrary networking fixes. Include recommendations for plain clear interface language and minimum dangerous-action confirmation scope.

Official public source facts checked today by the requesting reviewer:
- LizardByte/Sunshine master src/config.cpp defaults credentials_file to file_state. src/confighttp.cpp getPage redirects GET /welcome to '/' when a username is configured; otherwise serves the welcome page. authenticate redirects missing username to /welcome and challenges missing Authorization when a username exists.
- Sunshine src/nvhttp.cpp save state has root.uniqueid for server identity and root.named_devices for clients with cert/name/uuid/enabled; legacy root.devices has uniqueid/certs. A recursive uniqueid count can count the host.
- moonlight-stream/moonlight-ios master currently has no CFBundleURLTypes/CFBundleURLSchemes or inbound openURL handler found by repository-wide search; do not invent a Moonlight URL scheme.
- App Store id1000551566 is the canonical Moonlight Game Streaming iOS app.

Useful official URLs for references, not proof that you fetched them: https://github.com/LizardByte/Sunshine ; https://docs.lizardbyte.dev/projects/sunshine/latest/md_docs_2api.html ; https://github.com/moonlight-stream/moonlight-ios ; https://github.com/moonlight-stream/moonlight-docs/wiki/Setup-Guide .

Structure: 1) ranked correctness/security pitfalls with failure scenario and remedy; 2) recommended architecture and lifecycle invariants; 3) quick and guided user paths; 4) practical regression matrix including fault injection; 5) physical-device tests still required; 6) uncertain claims to check against official source. This is advisory and will be verified by another engineer before adoption.
